# Copie para .env e preencha: cp .env.example .env
# Para o SITE (Netlify), configure as mesmas variáveis no painel do Netlify (Site settings → Environment variables).

# Groq — geração e reflexão do livro
GROQ_API_KEY=sua_chave_aqui
GROQ_MODEL=llama-3.3-70b-versatile

# Azure Speech (Cognitive Services) — TTS nas meditações (voz neural pt-BR)
# Chave 1 do recurso "Speech" no portal Azure; região ex.: brazilsouth
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=brazilsouth
# Opcional: voz (padrão FranciscaNeural)
# AZURE_SPEECH_VOICE=pt-BR-FranciscaNeural
