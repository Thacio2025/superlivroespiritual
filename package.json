{
  "name": "super-livro",
  "private": true,
  "description": "Livro infinito espiritual — site e funções Netlify",
  "scripts": {
    "build": "cd app && npm run build",
    "dev": "netlify dev",
    "preview": "cd app && npm run preview"
  },
  "devDependencies": {
    "@netlify/functions": "^2.8.0",
    "typescript": "^5.3.0"
  }
}
